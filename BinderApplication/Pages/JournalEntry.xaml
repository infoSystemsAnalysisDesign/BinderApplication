<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BinderApplication.Pages.JournalEntry"
             Title="Binder">
    <!--<VerticalStackLayout>
        <Label 
            Text="Your Reading Binder"
            FontSize="Large"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>-->
    <!-- JournalEntryPage.xaml -->
<!--<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="YourNamespace.JournalEntryPage">-->

    <!--<StackLayout>
        --><!-- Add your UI elements for journal entry input here --><!--
        <Entry Placeholder="Type your journal entry..." x:Name="entryJournal" />
        <Button Text="Save" Clicked="OnSaveClicked" />
    </StackLayout>-->
    <StackLayout>
        <!-- Add your UI elements for journal entry input here -->
        <Entry Placeholder="Type your journal entry..." x:Name="entryJournal" />
        <Button Text="Save" Clicked="OnSaveClicked" />
    </StackLayout>
</ContentPage>
